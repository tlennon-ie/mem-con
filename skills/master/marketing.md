---
name: marketing
description: >
  Load Marketing team context and run the Discover → Define → Build → Review → Capture
  workflow. Trigger: /marketing, "Marketing context", "load marketing"
---

# Marketing Team Skill

1. Identify the relevant area within the Marketing team (areas: content, demand-gen, brand)
2. Write the context id to `.memcon-context`
3. Load `memory/shared/` then `memory/marketing/{area}/`
4. Run the Discover → Define → Build → Review → Capture workflow
5. Build artifact format adapts to user role within Marketing context
