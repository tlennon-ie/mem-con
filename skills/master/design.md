---
name: design
description: >
  Load Design & UX team context and run the Discover → Define → Build → Review → Capture
  workflow. Trigger: /design, "Design & UX context", "load design"
---

# Design & UX Team Skill

1. Identify the relevant area within the Design & UX team (areas: ux-research, product-design, design-system)
2. Write the context id to `.memcon-context`
3. Load `memory/shared/` then `memory/design/{area}/`
4. Run the Discover → Define → Build → Review → Capture workflow
5. Build artifact format adapts to user role within Design & UX context
