---
name: engineering
description: >
  Load Engineering team context and run the Discover → Define → Build → Review → Capture
  workflow. Trigger: /engineering, "Engineering context", "load engineering"
---

# Engineering Team Skill

1. Identify the relevant area within the Engineering team (areas: platform, frontend, backend, devops)
2. Write the context id to `.memcon-context`
3. Load `memory/shared/` then `memory/engineering/{area}/`
4. Run the Discover → Define → Build → Review → Capture workflow
5. Build artifact format adapts to user role within Engineering context
